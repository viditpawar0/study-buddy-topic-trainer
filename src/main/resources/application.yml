server:
  port: 8082
spring:
  application:
    name: topic-trainer
  datasource:
    driver-class-name: org.postgresql.Driver
    password: boot
    url: jdbc:postgresql://localhost:5432/sb_topic_trainer
    username: springboot
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  ai:
    openai:
      base-url: http://127.0.0.1:1234
      api-key: xx